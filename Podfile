# Uncomment the next line to define a global platform for your project
platform :ios, '10.3'

source 'https://github.com/CocoaPods/Specs.git'

target 'clientLeger' do
  use_frameworks!
  
  pod 'Socket.IO-Client-Swift', '~> 11.1.2'
  pod 'Alamofire', '~> 4.5'
  pod 'AEXML'
  pod 'FileBrowser', '~> 1.0'
  pod 'SwiftySound'
  pod ‘iCarousel’

  # Pods for clientLeger

  target 'clientLegerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'clientLegerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
